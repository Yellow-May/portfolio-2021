import { DefaultTheme } from "styled-components";

export const currentTheme = (theme: "dark" | "light") => {
	let defaultTheme = {} as DefaultTheme;

	if (theme === "light") {
	}

	return defaultTheme;
};

export default currentTheme;
